# Local development (default — keep localhost)
DATABASE_URL="mysql://root:password@localhost:3306/mind_heal"

# Production / hosted API: copy to .env.production (gitignored). URL-encode special chars in password (! @ #).
# DATABASE_URL="mysql://DB_USER:DB_PASSWORD_ENCODED@194.59.164.73:3306/u108457690_mhdb"
#JWT_SECRET="change-this-to-a-long-random-secret-key"
JWT_SECRET="mind-heal-long-random-secret-key-root-shift-hash-value"
JWT_EXPIRES_IN="7d"
PORT=4000
CLIENT_URL="http://localhost:5175"
